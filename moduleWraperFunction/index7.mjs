(function(exports, require, module, __filename, __dirname){// module wraper function every thing in it is private
    //const a=require("fs")->behind the scene ye code yehai hota hai
    const name="chandu"
    console.log(name)
    //module.exports={scffdsdvdsds} -> behind the scene this code will here onley
})();